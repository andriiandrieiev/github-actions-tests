name: Minisign test

on:
  push:

  workflow_dispatch:

env:
  GITHUB_TOKEN: ${{ secrets.CUSTOM_GITHUB_TOKEN }}

jobs:
  minisign-test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:

      - name: Set VyOS version
        id: set_vyos_version
        run: |
          echo "VYOS_VERSION=1.4-rolling-$(date -u +%Y%m%d%H%M)" >> $GITHUB_ENV

      - uses: actions/checkout@v3

      - run: |
          echo $VYOS_VERSION > vyos-$VYOS_VERSION-amd64.iso

      - name: Check ISO
        uses: cross-the-world/ssh-scp-ssh-pipelines@latest
        env:
          VYOS_VERSION: ${{ env.VYOS_VERSION }}
        with:
          host: ${{ secrets.SSH_HOST }}
          user: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_KEY }}
          connect_timeout: 10s
          first_ssh: |
            docker ps
          scp: |
            './vyos-$VYOS_VERSION-amd64.iso' => /tmp/
          last_ssh: |
            cat /tmp/vyos-$VYOS_VERSION-amd64.iso