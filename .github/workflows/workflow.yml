name: Fail test workflow

on:
  push:
  workflow_dispatch:
    inputs:
      DO_SMOKETEST:
        description: 'False to skip smoketest'
        required: true
        type: boolean
        default: true

jobs:
  job1:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:

      - run: |
          echo ${{ inputs.DO_SMOKETEST }} 
          
      - run: |
          echo TEST!!!
        if:  ${{ inputs.DO_SMOKETEST }} 

