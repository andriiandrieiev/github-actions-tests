name: Caller workflow

on:
  workflow_dispatch:
  push:

jobs:
  caller_workflow:
    runs-on: ubuntu-latest
    steps:


      - name: touch test.txt
        run: |
          pwd
          echo BLABLA ./test.txt
          cat ./test.txt

      - name: Run SSH test
        uses: cross-the-world/ssh-scp-ssh-pipelines@latest
        with:
          host: ${{ secrets.SSH_HOST }}
          user: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_KEY }}
          connect_timeout: 10s
          first_ssh: |
            whoami
            echo LALALA_1
          scp: |
            './test.txt' => '~/target-folder/'
          last_ssh: |
            echo LALALA_2
            cat ~/target-folder/test.txt